// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // TODO: switch to https://noworriesnate.com once the Porkbun transfer
  // completes and DNS points at GitHub Pages (also restore public/CNAME).
  site: 'https://nloum.github.io',
});
